[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "tickrush"
version = "0.1.0"
description = "High-performance Betfair price stream reader"
authors = [{name = "Maurice Berk", email = "maurice@mauriceberk.com"}]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Rust",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = []

[project.urls]
Repository = "https://github.com/mberk/tickrush"

[dependency-groups]
dev = [
    "pytest>=7.0",
    "betfairutil[files]>=0.5.0",
    "setuptools>=80.10.2",
]
benchmark = [
    "flumine>=2.11.5",
    "python-json-logger>=2.0.7",
]

[tool.uv]
default-groups = ["dev"]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"
module-name = "tickrush.tickrush"

[tool.pytest.ini_options]
testpaths = ["tests"]
